name: homonyms
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - numpy
  - matplotlib
  - scikit-learn
  - jupyterlab
  - ipyparallel
  - spacy
  - pip
  - pip:
      - transformers==4.30.0
      - torch>=1.12.0
      - tqdm
